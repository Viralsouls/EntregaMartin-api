<h2>Nuestros Productos 🛍️</h2>

<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; margin-bottom: 20px;">
    {{#each products}}
        <div class="product-card" style="border: 1px solid #ccc; padding: 15px; border-radius: 8px;">
            <h4>{{this.title}}</h4>
            <p><strong>Categoría:</strong> {{this.category}}</p>
            <p><strong>Precio:</strong> ${{this.price}}</p>
            <p><strong>Stock:</strong> {{this.stock}}</p>
            <button>Agregar al Carrito</button>
        </div>
    {{/each}}
</div>

<div class="pagination" style="text-align: center;">
    {{#if hasPrevPage}}
        <a href="{{prevLink}}" style="margin-right: 15px;">⬅️ Anterior</a>
    {{/if}}
    <span style="font-weight: bold;">Página {{page}} de {{totalPages}}</span>
    {{#if hasNextPage}}
        <a href="{{nextLink}}" style="margin-left: 15px;">Siguiente ➡️</a>
    {{/if}}
</div>